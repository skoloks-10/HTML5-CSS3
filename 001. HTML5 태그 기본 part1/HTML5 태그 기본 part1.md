# HTML5 태그 기본 part1

Date: 2025년 2월 26일
페이지: P.35~P.70
완료: Yes

- 태그와  요소
    
    `<h1>시작태그 </h1>끝태그`
    
    일부태그는 태그 내부에 다른 태그를 삽입할 수 있다.
    
- 속성
    
    `<h1 title=”heder”>title을 속성이름 header를 속성값으로 표현.`
    
- 주석
    
    일반적인 슬래쉬 주석과 달리 <!— —>로 표기.
    
    emmet 명령어는 ctrl+/
    
- 페이지구조
    
    ```html
    <!DOCTYPE html>
    <html lang="ko-KR">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title></title>
      </head>
      <body></body>
    </html>
    ```
    
    1줄 현재 버전을 의미, 반드시 있어야함.
    
    2줄 어느 언어로 만들어져있는지 표시
    
    `<head>` 바디에 필요한 스타일시트와 자바스크립트 기입
    
    `<title>` 제목
    
    `<body>` 실제로 보이는 영역
    
- 제목태그
    
    h1~h6 제목사용태그 숫자 순서대로 사용하며 h1은 한 화면에 한개
    
- 본문태그
    
    p  단락생성
    
    br 줄바꿈
    
    hr 수평줄
    
    a 앵커태크 링크역할
    
    링크기능을 없애고싶다면 href=”#”
    
- 글자태그
    
    b 굵은글자
    
    i 기울어진글자
    
    small 작은글자
    
    sub 아래첨자
    
    sup 윗첨자
    
    ins 밑줄
    
    del 취소선
    
- 목록태그
    
    ul 순서가 없는 목록 (점으로)
    
    ol 순서가 있는 목록 (숫자등)
    
    li 목록 요소
    
- 정의태그
    
    dl 정의목록
    
    dt 정의용어
    
    dd 정의설명
    
- 테이블태그
    
    `<table></table>`사용
    
    tr 행 
    
    th 행제목
    
    td 행내용
    
    border 표 테두리 두께
    
    `<table border=”3”>`의 형태
    
    `<th colspan=”3”>` 셀넓이
    
    `<th rowspan=”3”>` 셀 높이
    
- 이미지태그
    
    `<img src=”apple.png” alt=”사과” width=”300” height=”150”>`
    
    src 이미지경로
    
    alt 이미지가 없을때 출력
    
    dummyimage.com을 이용 더미이미지
    
- 오디오태그
    
    ```html
    <audio controls>
            <source src="https://www.w3schools.com/html/horse.mp3" type="audio/mpeg" />
            Your browser does not support the audio element.
        </audio>
    ```
    
    src 음악경로
    
    preload 재생하기전에 모두 불러오기
    
    autoplay 자동재생
    
    loop 반복재생
    
    controls 재생도구출력
    
    type 태그는 트래픽낭비 방지를 위함.
    
- emmet
    
    h1  `<h1></h1>`
    
    h1+h2 `<h1>…</h2>`
    
    h$*6 `<h1><h2>…</h6>`
    
    h${hello}*6 `<h1>hello</h1>..hello</h6>`
    
    div.one.two `<div class=”one two”></div>`
    
    div#one.two.three  `<div id="one" class="two three"></div>`
    
- 유용한 단축키
    
    동시편집 ctrl+alt+방향, alt+클릭
    
    설정 ctrl+,
    
    터미널 ctrl+`
    
    사이드바 ctrl+b
    
    동시편집 ctrl+d
    
    주석 ctrl+/
    
    내어쓰기 shift+tab
    
    커서가 뒤에있을땐 ctrl+] or [
    
    한줄삭제 shift+del
    
- 퀴즈와 체크리스트

# 퀴즈와 체크리스트

---

## 1. 태그와 요소

### 퀴즈

- **퀴즈 1:** 태그(tag)와 요소(element)의 차이점은 무엇인가요?
- **퀴즈 2:** HTML에서 어떤 태그가 다른 태그 내부에 삽입될 수 있는지 예를 들어 설명해 보세요.
- **퀴즈 3:** `<h1>시작태그</h1>`에서 시작태그와 종료태그의 역할은 무엇인가요?

### 체크리스트

- [ ]  태그는 시작태그와 종료태그로 구성된다는 것을 이해하고 있는가?
- [ ]  요소와 태그의 관계를 스스로 설명할 수 있는가?
- [ ]  중첩 가능한 태그와 그렇지 않은(tag cannot contain certain elements) 경우를 구분하고 있는가?

---

## 2. 속성

### 퀴즈

- **퀴즈 1:** HTML 태그에서 속성이란 무엇이며, 속성값은 어떻게 표현하는지 설명해 보세요.
- **퀴즈 2:** `<h1 title="header">`에서 `title` 속성이 가진 역할은 무엇인가요?

### 체크리스트

- [ ]  주요 속성(예: id, class, title 등)의 기본 역할을 스스로 설명할 수 있는가?
- [ ]  속성값을 따옴표('' 또는 "")로 감싸는 이유를 이해하고 있는가?
- [ ]  예제 코드를 보며 올바른 구문으로 속성을 사용했는지 확인했는가?

---

## 3. 주석

### 퀴즈

- **퀴즈 1:** HTML 주석은 어떤 문법으로 작성하나요?
- **퀴즈 2:** 일반적인 프로그래밍의 슬래시 주석과 HTML 주석의 차이를 설명해 보세요.

### 체크리스트

- [ ]  HTML 주석 작성 시 `<!--` 와 `->` 를 올바르게 사용하고 있는가?
- [ ]  주석을 활용해 노트를 정리할 때 핵심 정보를 분리해 표시했는가?

---

## 4. 페이지 구조

### 퀴즈

- **퀴즈 1:** HTML 기본 문서 구조에서 반드시 포함해야 하는 요소들은 무엇인가요?
- **퀴즈 2:** `<html lang="ko-KR">`의 `lang` 속성은 왜 중요한지 설명해 보세요.

### 체크리스트

- [ ]  `<!DOCTYPE html>` 선언이 포함되어 있는가?
- [ ]  `<html>`, `<head>`, `<body>`와 같은 기본 요소들이 올바르게 배치되어 있는가?
- [ ]  `<meta charset="UTF-8">`와 `<meta name="viewport">` 등 필수 메타 태그들이 존재하는가?

---

## 5. 제목 태그

### 퀴즈

- **퀴즈 1:** `h1`부터 `h6`까지의 제목 태그는 어떤 용도로 사용되며, 각각의 중요도는 어떻게 구분되나요?
- **퀴즈 2:** 한 페이지에서 `h1` 태그를 왜 하나만 사용하는 것이 좋은지 설명해 보세요.

### 체크리스트

- [ ]  제목 태그가 계층적으로 구성되어 전체 문서의 구조를 잘 나타내고 있는가?
- [ ]  각 제목 태그의 용법과 스타일 차이를 이해하고 있는가?

---

## 6. 본문 태그

### 퀴즈

- **퀴즈 1:** `<p>`, `<br>`, `<hr>` 태그의 역할과 사용법을 각각 설명해 보세요.
- **퀴즈 2:** `<a>` 태그를 이용한 링크 작성 시, 링크 기능을 없애고 싶다면 어떤 속성을 사용해야 하는지 설명하세요.

### 체크리스트

- [ ]  본문 작성 시 적절한 태그들을 사용하고 문단과 줄바꿈을 구분했는가?
- [ ]  링크 태그에서 `href="#"`와 같이 속성을 활용해 링크 비활성화가 가능한지 확인했는가?

---

## 7. 글자 태그

### 퀴즈

- **퀴즈 1:** `<b>`, `<i>`, `<small>`, `<sub>`, `<sup>`, `<ins>`, `<del>` 태그가 각각 문서에서 어떤 스타일을 적용하는지 설명해 보세요.
- **퀴즈 2:** 문서에서 특정 텍스트에 강조나 삽입, 취소선 효과를 주기 위한 용도로 어떤 태그를 사용하는지 예를 들어 보세요.

### 체크리스트

- [ ]  각 글자 태그의 효과를 실제 텍스트에 적용해보고 결과를 확인했는가?
- [ ]  사용하는 태그가 문맥에 맞는지 확인해 보았는가?

---

## 8. 목록 태그

### 퀴즈

- **퀴즈 1:** `<ul>`과 `<ol>`의 차이점을 설명하고, 각 목록에 어떤 태그를 사용하여 목록 항목을 추가하는지 말해보세요.
- **퀴즈 2:** 언제 순서가 없는 목록과 순서가 있는 목록을 사용해야 하는지 예시를 들어 설명하세요.

### 체크리스트

- [ ]  목록을 작성할 때 `<ul>` 또는 `<ol>` 그리고 `<li>` 태그를 올바르게 사용했는가?
- [ ]  목록이 논리적 순서와 계층을 잘 표현하는지 확인했는가?

---

## 9. 정의 태그

### 퀴즈

- **퀴즈 1:** `<dl>`, `<dt>`, `<dd>` 태그를 사용하여 정의 목록을 구성하는 방법을 설명해 보세요.
- **퀴즈 2:** 정의 용어와 정의 설명이 올바른 태그로 구분되어 있는지 확인하는 방법은 무엇인가요?

### 체크리스트

- [ ]  정의 목록이 `<dl>` 태그 아래에 `<dt>`와 `<dd>`로 형식화되어 있는가?
- [ ]  각 용어와 설명을 명확하게 분리하여 작성했는가?

---

## 10. 테이블 태그

### 퀴즈

- **퀴즈 1:** `<table>`, `<tr>`, `<th>`, `<td>` 태그의 역할과 차이점을 설명해 보세요.
- **퀴즈 2:** `border`, `colspan`, `rowspan` 속성이 테이블 구성에 어떠한 영향을 미치는지 설명해 보세요.

### 체크리스트

- [ ]  테이블의 기본 구조가 `<table>`로 시작하여 `<tr>`, `<th>`, `<td>`로 올바르게 구성되었는가?
- [ ]  `border` 및 합치기 속성(`colspan`, `rowspan`)이 제대로 사용되었는가?

---

## 11. 이미지 태그

### 퀴즈

- **퀴즈 1:** `<img>` 태그에서 `src`와 `alt` 속성의 목적은 무엇인가요?
- **퀴즈 2:** 더미 이미지(dummy image)를 사용할 때 어떤 서비스를 사용하는지 설명해 보세요.

### 체크리스트

- [ ]  이미지 경로(`src`)와 대체 텍스트(`alt`)가 정확하게 입력되었는가?
- [ ]  이미지 크기 조절(`width`, `height`) 속성이 적절하게 사용되었는가?

---

## 12. 오디오 태그

### 퀴즈

- **퀴즈 1:** `<audio>` 태그의 `controls`, `preload`, `autoplay`, `loop` 속성이 각각 어떤 역할을 하는지 설명해 보세요.
- **퀴즈 2:** `<source>` 태그의 `type` 속성이 중요한 이유는 무엇인가요?

### 체크리스트

- [ ]  오디오 태그가 올바르게 삽입되었으며, 필요한 속성(`controls`, `src`, `type`)이 포함되었는가?
- [ ]  오디오 재생이 정상적으로 작동하는지 테스트해 보았는가?

---

## 13. Emmet

### 퀴즈

- **퀴즈 1:** Emmet 단축어 `h$*6`를 입력하면 어떤 HTML 코드가 생성되는지 설명해 보세요.
- **퀴즈 2:** `div#one.two.three` 단축어의 결과물이 어떻게 나오는지 예시로 표현해 보세요.

### 체크리스트

- [ ]  Emmet 단축어로 원하는 HTML 요소가 빠르게 생성되는지 실습해 보았는가?
- [ ]  자주 사용하는 Emmet 단축어 목록을 만들어 두고 복습하고 있는가?

---

## 14. 유용한 단축키

### 퀴즈

- **퀴즈 1:** 동시 편집 단축키(예: `ctrl+alt+방향키` 또는 `alt+클릭`)는 어떤 상황에서 유용한지 설명해 보세요.
- **퀴즈 2:** 주석 단축키(`ctrl+/`)와 내어쓰기(`shift+tab`) 단축키의 사용법을 예로 들어 설명해 보세요.

### 체크리스트

- [ ]  각 단축키의 기능을 직접 사용해보며 익혔는가?
- [ ]  자주 사용하는 단축키의 목록을 만들어 연습하고 있는가?

---